//Can specify the return type of the function
function add(num1:number,num2:number):number{
    return num1+num2;
}

//If the function is not returning any value it type can be set as void or undefined 
//but we should never use undefined as a return type if it is not returning something always use void

//If function is not returning any value by default it is going to return undefined
 